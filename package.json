{
  "name": "nov13", //name of project
  "version": "1.0.0", // current version of project
  "main": "index.js", //Specifies the initial entry point for GAE
  "scripts": {
    "start": "node index.js",  // The command run by GAE to launch the server
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@google-cloud/vision": "^5.3.4", //Required library to interact with the Google Cloud Vision
    "multer": "^2.0.2", // To handle the multipart/form data file
    "express": "^4.19.2" // Web framework used to build server
  }
}
